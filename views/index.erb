<html>
<head>
  <meta charset="UTF-8">
</head>
<body>
  <h1>メモアプリ</h1>
  <div>
  <% @filenames.each do |title| %>
    <form method="get" action="/show_memo"> 
      <ul>
        <li><a href='/show_memo/<%= File.basename(title, ".*") %>'><%= @title = File.basename(title, ".*") %></a></li>
      </ul>
    </form>
  <% end %>
  </div>
  <div>
    <form method="get" action="/form">
      <input type="button" onclick="location.href='/form'" value="追加">
    </form>
  </div>
</body>
</html>
